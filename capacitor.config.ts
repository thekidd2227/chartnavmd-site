import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'npm install @capacitor/iosnpx cap add ios',
  webDir: 'dist'
};

export default config;
